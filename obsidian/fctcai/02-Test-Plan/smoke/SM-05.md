---
id: SM-05
name: Strategic Loop brain snapshot
layer: smoke
priority: P0
phases: [P4]
status: draft
created: 2026-04-29
estimated_effort_hours: 0.5
---

# SM-05 — Brain snapshot smoke

## Mục tiêu
`BrainStore.freeze()` không throw, snapshot có timestamp.

## Steps
1. Setup workspace
2. Call `BrainStore.getBrain(workspaceId)`
3. Call `BrainStore.freeze()`
4. Verify response: `{ snapshotId, frozenAt }`
5. Verify DB: `brain_snapshots` row có frozen_at

## Expected
- freeze() trả về snapshotId
- Timestamp populated

## Acceptance checklist
- [ ] freeze() returns snapshot info
- [ ] DB row tồn tại
- [ ] frozen_at là valid timestamp

## Implementation notes
**File:** `server/src/platform/strategic-loop/__tests__/brain-snapshot-smoke.test.ts`

**Risk:** `BrainStore.freeze()` chưa exist (cần verify) — có thể chỉ có `getBrain`/`appendInsight`. Cần spec rõ.

## Status
- [x] Draft
- [ ] Reviewed (cần verify API exists)
- [ ] Approved
- [ ] Implemented
